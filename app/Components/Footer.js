import React from "react";

import "../Game.css";

const Footer = () => {
  return (
    <div className="footer panel header">
      <button>New Game</button>
    </div>
  );
};

export default Footer;
